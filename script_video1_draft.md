First, for AI to do all the amazing it does, such as writing emial, wriing code... , i needs to learn from massive amount of data or text...

But the question is how does it learn from text ? 

here is a high level big picture that we will look into it in details as we progress with course 

first larger amout of data is split is small pieces called tokens, then thoses tokens are reprenset but an iD called token ID , those IDs are represented by list of numbers called embeddings, then thats we can start training by feeding these numbers in different mathemetical formulas called model to make prediction of a known answer, if model if far off the model internal keeps on be adjusted untill the predicted values are closer to the actual vlaues.

This high level view of the bigger picture , we will keep on revisiting , main propose of the course is to walk through each step in details and the this full picture is fully understood 

this lesson focus on the first step how do we convert/presesent text in numbers and mostly importantly why do we do that? 

Fact is machine learning  model or AI is a big mathemmetical machines , so  it needs numbers....
